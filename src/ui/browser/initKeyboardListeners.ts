// 写入 css 变量
export async function initKeyboardListeners() {
  // Tauri's WebView resizes with the virtual keyboard. The CSS dynamic viewport
  // units handle the layout without a platform-specific bridge.
}
