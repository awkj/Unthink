# Project Instructions

- This project does not require backward compatibility for existing local configuration keys or values. When configuration semantics change, update the key and implementation directly; do not add migration or legacy fallback logic unless explicitly requested.
